
  # Staff Scheduling Dashboard Prototype

  This is a code bundle for Staff Scheduling Dashboard Prototype. The original project is available at https://www.figma.com/design/R5ni024lljdH16WcW9DBAo/Staff-Scheduling-Dashboard-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  